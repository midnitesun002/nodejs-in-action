<entries>
    <% entries.forEach(entry => { %>
        <entry>
            <title><%= entry.title %></title>
            <body><%= entry.body %></body>
            <username><%= username %></username>
        </entry>
    <% }) %>
</entries>